/* The one place the app's version lives.

   The service worker imports this too, so the cache name and the version
   shown in the Parent Area can never drift apart: bump it here and the old
   cache is replaced automatically on the next launch.

   Bump the minor number for a change she would notice, the patch number
   for a fix. */

export const APP_VERSION = '1.32.0';
export const BUILD_DATE  = '2026-09-23';
